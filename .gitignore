# Dependencies
node_modules/

# Build output
dist/
.vercel/

# Environment
.env
.env.local
.env.*.local

# OS cruft
.DS_Store
Thumbs.db

# Claude Code local state — .claude/launch.json IS committed (shared dev-server
# config); per-machine settings and worktrees are not
.claude/settings.local.json
.claude/worktrees/

# Archives (a stray files.zip once got committed here)
*.zip

# Test / coverage output
coverage/

# Pipeline run artifacts — traces and token logs are per-run, not source
pipeline/runs/
evals/runs/

# Built bundle — `npm run build:chart`, and by Vercel on deploy
chart/
askbox/
